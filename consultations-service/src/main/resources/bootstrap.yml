#Application Name
spring:
  application:
    name: consultations-service     # This is the name for the remote configuration file
  cloud:
    config:
      uri: http://localhost:8888 #where the config-service is running
      fail-fast: true #the service will not run if it can't reach the config-service
  mail:
    host: smtp.gmail.com
    port: 587
    username: digitalholics.theraphy@gmail.com
    password: hafq njao nuxt ebvj
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
